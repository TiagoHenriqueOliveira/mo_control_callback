<button type="button"
    class="btn btn-sm btn-indigo btn-modal-natureza-atendimento"
    data-id="{{ $n->nat_aten_id }}"
    data-descricao="{{ e($n->nat_aten_descricao) }}"
    data-ativo="{{ $n->nat_aten_ativo }}">
    <i class="fas fa-edit"></i>
</button>